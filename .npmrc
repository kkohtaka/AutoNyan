# Fail loudly when the running Node does not satisfy the `engines` field in
# package.json, instead of installing anyway and silently rewriting the lockfile.
# The pinned version lives in `.nvmrc`; see REMOTE_SESSION_SETUP.md section 3.
engine-strict=true
