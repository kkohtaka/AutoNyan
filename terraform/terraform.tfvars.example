# Copy this file to terraform.tfvars and fill in your values
# DO NOT commit terraform.tfvars to version control - it contains sensitive data

project_id = "your-project-id"
region = "us-central1"
drive_folder_id = "your-google-drive-folder-id"
drive_scanner_schedule = "0 * * * *"

# File classification configuration
# category_root_folder_id: Google Drive folder containing category subfolders (e.g., "請求書", "契約書")
# uncategorized_folder_id: Google Drive folder for files that don't match any category
category_root_folder_id = "your-category-root-folder-id"
uncategorized_folder_id = "your-uncategorized-folder-id"

# Cost guardrail configuration
# billing_account_id: Cloud Billing account ID in the form XXXXXX-XXXXXX-XXXXXX
# budget_amount: monthly budget in whole units of the billing account's currency
#   (the budget inherits the billing account's own currency automatically)
# budget_alert_thresholds is optional (default: [0.5, 0.9, 1.0])
billing_account_id = "XXXXXX-XXXXXX-XXXXXX"
budget_amount = 10000
# budget_alert_thresholds = [0.5, 0.9, 1.0]

# Notification configuration
# Google Workspace email address used as the sender for processing notifications.
# After deployment, grant Domain-Wide Delegation to the notification dispatcher service account
# using the client ID from: terraform output notification_dispatcher_service_account_client_id
notification_from_email = "admin@your-workspace-domain.com"
